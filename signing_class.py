class Signing:
    def __init__ (signing, id, createdTime, finishTime, workplace, worker, hours):

        signing.id = id
        signing.createdTime = createdTime
        signing.finishTime = finishTime
        signing.workplace = workplace
        signing.worker = worker
        signing.hours = hours